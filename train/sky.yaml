resources:
  cloud: lambda
  accelerators: GH200:1

  # Disk size for model and data
  disk_size: 100

  # Auto-stop cluster after job finishes (saves money!)
  autostop:
    idle_minutes: 30
    down: true # Use autodown.

# Sync local code and data to cluster
workdir: .

# Setup commands (run once when cluster is created)
# GH200 is aarch64 (ARM) - PyPI has no CUDA torch wheels for ARM,
# so we use the system-installed python3-torch-cuda (apt) and install
# other deps via pip into the system Python.
setup: |
  cd ~/sky_workdir
  /usr/bin/python3 -m pip install accelerate bitsandbytes datasets jsonlines peft scikit-learn \
    sentencepiece tqdm 'transformers>=4.50.0,<4.57.0' trl wandb 'Pillow>=9.1.0' 'numpy<2' tf-keras
  # pip can't uninstall the apt torch, which is what we want - system CUDA torch stays

# Training command
run: |
  cd ~/sky_workdir
  export CUDA_VISIBLE_DEVICES=0
  export MODEL_NAME=${MODEL_NAME}
  export WANDB_PROJECT="lexide"
  export TOKENIZERS_PARALLELISM=false
  export HF_TOKEN=${HF_TOKEN}
  export WANDB_API_KEY=${WANDB_API_KEY}
  export HF_USERNAME=${HF_USERNAME}

  # Use system python (has CUDA torch from apt: python3-torch-cuda)
  /usr/bin/python3 -m accelerate.commands.launch \
    --mixed_precision bf16 \
    --num_processes 1 \
    --num_machines 1 \
    --dynamo_backend no \
    src/train.py

envs:
  # models: gemma-3-270m-it, gemma-3-1b-it, gemma-3-4b-it, gemma-3-12b-it, gemma-3-27b-it
  MODEL_NAME: gemma-3-27b-it
  HF_USERNAME: anchpop # Optional: auto-creates repo anchpop/lexide-MODEL_NAME on HuggingFace

secrets:
  HF_TOKEN: null
  WANDB_API_KEY: null

file_mounts:
  ~/.netrc: ~/.netrc # wandb
